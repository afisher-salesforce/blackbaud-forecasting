{
  "name": "blackbaud-forecasting-heroku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd artifacts/blackbaud-forecasting && npm install && npm run build",
    "heroku-postbuild": "npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
